# rov_lab

`rov_lab` contains the Isaac Lab-based underwater demonstration generation code for `rov_il`.

It owns the ROV/underwater task code and imports reusable functionality from `external_dependencies/leisaac` instead of modifying leisaac directly.

